describe('UserPage', () => {
  it('should render', () => {});

  it('should redirect to home page when member is not found', () => {});

  it('should only render messages from selected member', () => {});

  it('should render a full name', () => {});

  it('should render a return link', () => {});
});
